
export default async (app, req, res, next) => {
    try {
        
        
        // reads token from body 
        // validates token
        // checks if expired, if not, passes back accessToken from db
        // if expired, do token request to spotify
    } catch(ex) {
        
    }
};